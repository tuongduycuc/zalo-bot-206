# URL máy nội bộ của bạn (đã expose bằng cloudflared/localtunnel/ngrok)
# Ví dụ: https://yrs-initiated-inbox-cottages.trycloudflare.com/webhook
FORWARD_URL=

# (Tuỳ chọn) Chuỗi bí mật dùng để xác thực từ proxy đến bot của bạn
# Proxy sẽ gửi header: X-Proxy-Token: <PROXY_TOKEN>
PROXY_TOKEN=change-me

# Port khi chạy local (Render sẽ tự đặt)
PORT=3000
